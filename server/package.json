{
  "name": "server",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "generate-schema": "npx chanfana -o ../common/api/schema.json"
  },
  "dependencies": {
    "chanfana": "^3.3.0",
    "hono": "^4.12.0",
    "zod": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^7.0.0",
    "wrangler": "^4.120.0"
  }
}
